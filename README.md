# TinyDPPM

A small, from-scratch implementation of a **Denoising Diffusion Probabilistic Model (DDPM)** in PyTorch.

TinyDPPM is built for learning and experimentation rather than performance. The goal is to implement the core components of a diffusion model manually, including the noise scheduler, forward diffusion process, U-Net, training pipeline, and reverse diffusion process.
